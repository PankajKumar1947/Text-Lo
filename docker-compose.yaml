services:
  textlo:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      NODE_ENV: ${NODE_ENV}
    container_name: textlo
    restart: unless-stopped
    ports:
      - "3001:3001"
